{
  "name": "finance_tracker",
  "private": true,
  "scripts": {
    "dev:web": "pnpm --filter @finance-tracker/web dev",
    "dev:api": "pnpm --filter @finance-tracker/api dev",
    "dev:mobile": "REACT_NATIVE_PACKAGER_HOSTNAME=10.2.2.2 && pnpm --filter @finance-tracker/mobile android",
    "build": "pnpm -r build",
    "start": "next start",
    "lint": "eslint"
  }
}
